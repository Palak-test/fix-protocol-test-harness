# Developer Setup for Fixit

## Prerequisites
- Python 3.8+
- pip

## Setup
1. Clone the repository
2. Install dependencies:

    pip install -r requirements.txt

3. (Optional) For colored CLI output:

    pip install colorama

4. Run tests:

    pytest
